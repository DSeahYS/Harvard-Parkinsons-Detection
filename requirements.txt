pip install -r requirements.txt# Core computer vision and media processing
opencv-python>=4.7
mediapipe>=0.10

# Numerical operations
numpy>=1.20

# GUI support (Image handling)
Pillow>=9.0

# Configuration loading
python-dotenv>=0.20

# API requests (for LLM client)
requests>=2.25

# Add other specific dependencies if identified later
# e.g., scipy, scikit-learn if more complex analysis is added
